# 文档审计记录 · 2026-08-03（08-04 追加 D 类）

> 从 `00-总索引.md` 拆出。**仅用于追溯改动依据**，日常不需要读。
> 08-03 原始清单 15 项：A 类 3 项 + C 类 10 项已修，B 类 1 项加时效标注，C4 复核为误判已撤销。
> 08-04 追加 **D 类**：S1/S2 实现落地后的文档同步。

## 编号统一

2026-08-03 前并行 5 套编号（`P0~P5`、`v0.0a~v1.0`、`#1~#10`、补强优先级 `1~4`、废弃的 `v0.1~v0.5`），互不映射且都不表示执行顺序。现统一为单轴 `S1~S10`，序号即执行顺序，旧编号退为别名。

三个成因（均已消除）：

1. 阶段编号被当成执行顺序读 —— `P0~P5` 是阶段划分，但 S1（原 P4a）刻意排在 S6（原 P2）之前
2. 最高优先级项没有编号 —— 物理层重写原先散在讨论记录 §5、S1 交接说明第八节和 HTML 三处，无编号无计划。现为 **S2**
3. 两套优先级表序号冲突 —— HTML 的 `#1~#10` 与面试分析的 `1~4` 指同一批事情但序号不同

新增事项接 `S11` 往后排。不在 S 号里插字母（不要 `S2a`），拆分就改成两个连续 S 号并注明来源。已完成的 S 号不回收。

## A 类：决策已定但文档未同步（已修）

### A1 · P3 占点未同步取消

讨论记录 §5 决策 1 明确「砍掉占点」，但 Timeline 整节 + README + 状态帧同步总目录共 9 处仍原样保留。已改：

- `Gameplay状态同步演示-Timeline.md`：P3 整节改为 `~~P3~~（已取消）`，保留历史范围留档并注明「编号保留不复用」；Context、技术约束、P1 非目标、推荐开发顺序、面试讲解主线、P5 验证方式共 6 处的占点已换为击退
- `README.md`：玩法闭环第 4 条、展示重点、阶段摘要表共 3 处
- `../状态帧同步/状态帧同步-阶段总目录.md`：`:12` 求职分支说明

代码侧已核实：全仓库 grep `capture` / `占点` **零命中**，占点从未实现，纯文档残留。

### A2 · P2 范围未含击退

Timeline 的 P2（现 S6）原本只有 Dash/体力/Recover。已加：撞击击退（服务端权威、客户端故意不预测）、范围调整记录、对 S1 的前置依赖、以及**反向约束**——击退定稿后须回头复核平滑阈值 `6.0`，阈值必须大于单次最大击退距离，否则击退走硬跳分支会毁掉并排对比演示。

同时给 P2~P5 补上缺失的 `**状态**` 字段。

### A3 · Box2D 决策未同步

- `../状态帧同步/状态帧同步-阶段总目录.md` `:18`：「已锁定的技术决策：使用 Box2D 路线」改为划掉 + 标注 08-03 推翻
- `README.md` `:18`：原文「Box2D 用于确定性碰撞、地图边界和简单交互体」**三项都不成立**（无 Contact、边界靠手写 `ClampPlayersToRoom`、无交互体），已改为陈述求解器空转的事实

这条原先不只是过期，是**事实错误**。分支进度 HTML §3.4 已核实「四面墙是纯视觉，没有 Box2D body」。

## B 类：基线数据过期（已加时效标注）

### B1 · 面试问题应对分析（07-27 基线）

| 位置 | 文档说法 | 实际 |
|------|---------|------|
| `:7` | 缺口含「带宽观测」 | 已完成（`4c1104cb` + `70e9aad7`） |
| `:70` | 「带宽多少 → 答不上，零带宽统计代码」 | 同上，现在能答出数字 |
| `:119` | 补强优先级 1「带宽统计，半小时」 | 已做完 |
| `:127` | 「P2、P3 边际价值低」 | P3 已取消；P2 已重定范围含击退，价值上调 |
| `:105` | 禁区「不要说用定点数保证确定性…是混合架构」 | 决策 2 落地后此条会反转 |

**处理方式**：文首加时效标注，列出 4 处需就地换算的点。正文结论一律未改——分档与应答思路仍有效。

## C 类：客观错误（已全部修）

| # | 文件 | 原问题 | 处理 |
|---|------|--------|------|
| C1 | `README.md` | 基线命令 `--framework net8.0` 已失效（三个 csproj 是单目标 `net9.0`，`55d8c97d` 改的，系统 dotnet 报 `NETSDK1045`） | 换成 Rider dotnet 全路径，补「先停服务端」与只验证编译的命令 |
| C2 | `物理层与玩法形态-讨论记录-20260803.md` | 称 `FrameSyncPhysicsWorld.cs`「114 行」，实际 687 行 | 改为 687 行。结论（求解器空转）抽查无误 |
| C3 | `Box2D确定性分析/README.md` | 称「636 行」已过期 | 标注「成文时 636 行，现 687 行，下文行号为旧行号」 |
| C4 | ~~同上，教材路径错~~ | ~~路径不存在~~ | **误判，已撤销。** 两个 HTML 都真实存在 |
| C5 | 验收测试指南 | 文件名 `20260802`，内容实测基线是 08-03 | `git mv` 改名，更新 2 处引用 |
| C6 | 同上 | 排查列表出现两个 `12.` | 后一条改为 `13.` |
| C7 | 同上 | 写「注册三条新用例」，实际四条 | 改为四条，补注册位置 |
| C8 | 同上 | 列出「`TestRunner.cs` 加场景映射」，与交接说明「不用改」矛盾 | 删掉该行。`room-boundary-clamps-player` 在 `TestRunner.cs` grep 零命中，交接说明是对的 |
| C9 | S1 实现计划 | 同 C8 | 删掉 `TestRunner.cs`，改为显式声明「不需要改」+ 理由 |
| C10 | 同上 | 验证方式写「三条新用例」 | 改为四条 |
| C11 | `Gameplay状态同步演示-Timeline.md` | P2~P5 无 `**状态**` 字段 | 已补齐 |

## D 类：S1/S2 落地后的文档同步（2026-08-04）

`9be6e989` 完成 S1/S2 实现，但文档仍写「计划已就绪 / 下一项」。本轮同步：

| 文件 | 改动 |
|------|------|
| `00-总索引.md` | 项目现状改为 S1/S2 已完成、用例 19 → 25；S 表 S1/S2 划掉标完成，S3 升为下一项；`#6` epsilon 从「S2 后大概率作废」改为「已作废」；补 Box2D dll 遗留清理项；补 S6 击退需复核阈值 `6.0` 的反向约束；新增「实现报告」区收录两份 08-04 HTML；S1/S2 施工文档降级为「已完成，留作实现依据」 |
| `README.md` | 物理层段从「已决策拿掉」改为「已落地手写定点」；S 表同步；已完成清单补 S1/S2 与 25 条用例 |
| `Gameplay状态同步演示-Timeline.md` | P4 状态改为「S1 已完成，S9 未开始」；P↔S 映射表 S1 标完成；「紧邻三步」改为「已走完 S1/S2，下一步 S3」 |
| `面试问题应对分析.md` | 时效标注第 3 条禁区标为**已反转**（定点已落地）；第 4 条改为 S1/S2 已完成、下一项 S3；新增第 5 条——「权威修正是可见硬跳」已过期 |
| `../状态帧同步/状态帧同步-阶段总目录.md` | 决策 1 从「已推翻」补为「08-04 已落地」并指向 `FixedPhysicsBody.cs` |
| `S1-实现计划` / `S1-交接说明` / `S2-实现计划` | 文首加完成标注。S1 记录「计划四条用例、实际五条」；S2 记录「预期 400~500 行、实际 102 行」 |
| 两份验收指南 | `cd` 路径 `D:/unity/Tencent/TEngine` → `D:/Unity/WeDoBest`（旧路径跑不通）；「四条新用例」→ 五条；补 `error-smoothing-matching-snapshots-decay` 的失败含义 |

**未改的**：历史计划文档（`../状态帧同步/v0.x`、`CLI游戏逻辑测试应用程序-实施计划.md`）里的旧路径 markdown 链接。那些是留档，不作为执行依据。

**实现与计划的两处偏差**（已在各文档就地标注）：

1. S1 落地五条用例而非计划的四条。多出的 `error-smoothing-matching-snapshots-decay` 对应一个计划阶段没预见的缺陷：一致快照也会调 `CapturePredictionErrorAfterReconciliation()`，导致每个 30Hz 快照都重启平滑计时器、偏移永不归零。修法是给该调用加 `logRollback` 守卫
2. `FrameSyncPhysicsWorld.cs` 缩到 102 行，远低于计划预估的 400~500 行

## 已核实一致、不是冲突的

- **S1（原 P4a）先于 S6（原 P2）执行** —— 刻意拆分，Timeline 与讨论记录 §6 两条独立分析线得出同一结论
- **现有 19 个自测用例** —— `AllCaseNames` 正好 19 条
- **`BattleSimulation.cs` 行号锚点** —— `:38` `:102-117` `:415` `:436` `:543` 全部命中
- **`BattleClientController.cs` 行号锚点** —— `:145` `:163` `:192` `:432` `:772` `:796` 全部命中
- **`Entity.csproj`** —— `:31` GameShared 通配符、`:32-33` GameLogic 显式列举，新增 GameLogic 文件确实必须手动加行
- **两份 08-02 HTML 的 08-03 修订** —— 讨论记录 §7 声称的 8 处修改，抽查一致
