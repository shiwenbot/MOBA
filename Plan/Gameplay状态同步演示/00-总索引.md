# Gameplay 状态同步演示 · 总索引

> 当前分支 `feature/gameplay-sync-sandbox` 的文档入口。更新于 2026-08-04（S3 落地后）。

## 项目现状

**S1、S2、S3 已于 2026-08-04 完成**（`9be6e989`、`ee77a7a8`、`4e294d55`）。小球移动的预测-回滚最小闭环（`55d8c97d`）、带宽统计（`4c1104cb` + `70e9aad7`）之上，现在误差修正走多帧平滑、物理层全定点、快照通路无损且哈希双向对账，**29 条自测用例**（原 19 条 + S1 五条 + S2 一条 + S3 四条）。

**下一项是 S4 弱网自动化测试**，计划已就绪。弱网模拟设施目前**完全不存在**（全仓 `latency` / `jitter` / 丢包零命中），而 S3 完成标准里的两项、S1 的平滑阈值、击退收敛都只在弱网下才有观测意义，所以它从原 S7 提前并改号为 S4。**未开始的阶段已连带重编号，见「重编号对照表」。**

三项技术决策（出处：[物理层与玩法形态-讨论记录-20260803.md](物理层与玩法形态-讨论记录-20260803.md) 第五节）：

1. **玩法砍掉占点** —— 进度条看不出同步难点，改用撞击击退作为「服务端权威、客户端故意不预测」的对照演示。**待 S6 实现**
2. **拿掉 Box2D** —— 原实现 `GroupIndex=-1` 让 Contact 恒空，求解器空转，只剩浮点积分却背全部代价。**已落地**
3. **加预测误差平滑** —— 硬跳修正在演示里太扎眼，改为多帧收敛 + Ghost 可视化对比。**已落地**

## 已落地的实现

**S1 误差平滑**（`PredictionErrorSmoother.cs`）：修正从瞬时位移改为误差量衰减。逻辑状态立刻采用权威值，表现层保留误差偏移并在 `SmoothingDurationSeconds = 0.12f` 内衰减到零，超过 `MaxSmoothingDistance = 6.0f` 才走硬跳。Ghost 显示未平滑的原始权威位置用于并排对比。

**S2 物理层重写**（`FixedPhysicsBody.cs` + 重写的 `FrameSyncPhysicsWorld.cs`）：Box2D 依赖已拆除，`UnityProject/Assets/GameScripts` 与三个手工维护的服务端 csproj 中 `Box2DSharp` 零命中。`FrameSyncPhysicsWorld.cs` 从 687 行缩到 102 行，全 `Fixed64` 定点。新增 `fixed-physics-bit-exact` 用例。

因此 `#6`（误差判定加 epsilon）**已正式作废** —— 全定点后无浮点噪声，任何状态差异都是真失同步。

**S3 哈希上报与服务端告警**（`4e294d55`）：三件事一起落地。

1. **快照通路改无损** —— proto 的 `PlayerSnapshot` 与 `C2B_JoinBattleResponse` 坐标/速度改传 `m_rawValue`（`XRaw`/`YRaw`/`LinearVelocityXRaw`/`LinearVelocityYRaw`），四个死字段（`Angle`/`AngularVelocity`/`IsAwake`/`IsEnabled`）从 proto 删除、字段号 5/8/9/10 留空不复用。`GetSpawnPosition` 返回类型改 `(Fixed64, Fixed64)`。proto 内 `float` 仅剩输入的 `Dx`/`Dy`（本就无损）。**原先「往返丢约 13 位、`CheckConsistency` 严格相等几乎恒失配」的地基问题已消除。**
2. **客户端逐帧上报** —— `C2B_StateHashReport(FrameIndex, StateHash)`，采样点在 `BattleSimulation.Tick` 内 `AdvancePredictionTo` 之后当场算当场发，帧号与哈希同源，每 30 帧一次。不带任何客户端自算 RTT（S5 的反外挂价值靠服务端独立测量）。
3. **服务端历史索引与告警** —— `SnapshotBuffer<ulong> _authoritativeHashHistory`（容量 64）、`TryCompareReportedHash` 三态返回、`[Battle][HashMismatch]` 告警、`HashMismatchCount` / `HashNoRecordCount` 计数器。noRecord 不告警。

**S3 的两项真机观测仍未回填**（验收指南第 9 节）：真实网络下告警是否为零、`rollbackCount` 是否显著低于修复前。后者用于确认「修复前每个权威快照都触发一次回滚」这个**目前仍是推论**的判断。**这两项已列入 S4 的 Step 8**，弱网设施到位后一并回填。

**遗留清理项**：`UnityProject/Assets/GameScripts/HotFix/ThirdParty/Box2DSharp/Box2DSharp.dll` 本体仍在。这是 S2 计划的刻意安排（计划 `:189`），等无头 + 真机验收都通过后再删，方便回退。删除后 Unity 会自动重生成根目录 `.csproj`。

## 执行顺序

序号即执行顺序。

| S | 事项 | 成本 | 状态 |
|---|------|------|------|
| ~~S1~~ | 预测误差平滑 + Ghost 可视化 | 半天 | **已完成**（`9be6e989`） |
| ~~S2~~ | 物理层重写：拿掉 Box2D，手写定点 | 1-2 天 | **已完成**（`9be6e989`），dll 本体待删 |
| ~~S3~~ | 客户端哈希上报 + 服务端告警 | 约 1.5-2 天 | **已完成**（`4e294d55`），真机观测待回填 |
| **S4** | **弱网自动化测试（延迟/抖动/丢包）** | 1-2 天 | **下一项，计划已就绪**。原 S7，见重编号对照表 |
| S5 | 输入帧号 RTT 交叉校验（±200ms 窗口） | 未估 | 未开始，缺整条 RTT 通路；**须在 S4 之后**。原 S4 |
| S6 | 一致时跳过重放 | 半天 | 未开始，已部分实现。原 S5 |
| S7 | Dash / 体力 / Recover / 撞击击退 | 未估 | 未开始。原 S6 |
| S8 | 断线重连最小版 | 2-3 天 | 未开始 |
| S9 | 远端插值 + 轨迹线 + 完整调试面板 | 未估 | 未开始 |
| S10 | 求职展示打磨（README / 录屏 / 讲解） | 未估 | 未开始 |

已废止：`P3` 占点（职责移交 S7 击退）、`#6` epsilon 豁免（**S2 完成，已作废**）、`v0.1~v0.5` 弹珠路线。

S7 击退定稿后须回头复核平滑阈值 `MaxSmoothingDistance = 6.0f`：阈值必须大于单次最大击退距离，否则击退会走硬跳分支，毁掉并排对比演示。

### 重编号对照表（2026-08-04）

弱网测试提前为下一项后，为保持「序号即执行顺序」，未开始的阶段循环左移。**已完成的 S1~S3 不改号** —— 提交信息（`9be6e989` 的「完成 S1/S2」）与验收指南文件名都引用了它们。

| 旧号 | 新号 | 事项 |
|------|------|------|
| S7 | **S4** | 弱网自动化测试 |
| S4 | **S5** | RTT 交叉校验 |
| S5 | **S6** | 一致时跳过重放 |
| S6 | **S7** | Dash / 体力 / Recover / 撞击击退 |
| S1~S3、S8~S10 | 不变 | |

**S6 与 S7 两个号都改了含义**（旧 S6 击退 → 新 S7；旧 S7 弱网 → 新 S4）。这是本次重编号唯一的风险点 —— 同一个号指向了不同的事。

**本仓库内的 md 与 html 已全部就地更正**（含 S1/S2/S3 计划、Timeline、README、两份验收指南、四份 HTML 报告），无需换算。

需要换算的只有**仓库外的东西**：git 提交信息、以及 08-04 之前的对话记录。它们里的 S6/S7 按上表反查。

### 排序说明：为什么弱网测试提前

原表把弱网自动化（原 S7）排在最后一档。**S3 落地后这个顺序已调整为 S4**，理由是依赖方向反过来了：

- **S3 的完成标准有两项无法在零延迟下验证** —— 「真实网络下告警是否为零」「noRecord 占比不构成多数」都需要真实的 RTT 才有信号。无头测试客户端与服务端同进程，`noRecord` 恒为 0，等于这两条判据当前是空转的
- **S5（RTT 校验）在零延迟下无意义** —— 「输入帧号 RTT 交叉校验（±200ms 窗口）」的判据本体就是延迟。没有可控延迟注入，它既写不出有效用例，也证明不了拦得住什么
- **S1 的平滑层至今没有真实误差可吸收** —— `SmoothingDurationSeconds = 0.12f` 与 `MaxSmoothingDistance = 6.0f` 两个数值是照经验定的，本地零延迟下预测几乎不偏，参数是否合适**没有任何观测支撑**
- **S7 的击退收敛同样如此** —— 击退是「服务端权威、客户端故意不预测」的对照演示，它的说服力全部来自弱网下的误差可见与最终收敛。零延迟下击退看起来和预测过的没区别，演示价值归零
- **Timeline 的 P4 完成标准直接写了「使用本地弱网工具施加延迟/抖动/丢包」** —— 这个工具从来没有实现过

即弱网设施是 S1/S3/S5/S7 共同的观测前提，而不是它们之后的补充测试。**先建注入设施，再让后续每个阶段都在弱网下验收**，比先堆四个阶段再回头补测试省返工。

代价是 S7（Dash/击退，唯一的玩法增量）继续后延。若优先级是「尽快让 demo 画面上有更多东西」，则应先做 S7——但那样 S1 参数复核与 S3 真机观测会一直挂着。

### S5/S6/S7 的前置缺口

动手前必须先补，原估时未覆盖：

- **S5（RTT 校验，原 S4）缺整条 RTT 数据通路** —— `C2B_PingHandler.cs` 全文 17 行，只把时间戳原样塞回 Pong，不记录不计算，服务端侧 grep `rtt` 零命中（唯一命中是 `ServerTickDriver.cs:33` 的 `StartTick`，属误匹配）。「服务端用 RTT 反推应然帧号」的前提根本不存在。注意别让客户端上报自算的 `_rttEmaMs`——那会变成用客户端声称的 RTT 验证客户端声称的帧号，反外挂价值归零。必须服务端独立测量
- **S6（跳过重放，原 S5）已部分实现** —— 一致路径下计时、`_rollbackCount++`、日志已跳过（调用点传的 `logRollback` 就是 `mismatch`）。还在跑的是 `ApplyAuthoritativeSnapshot()` 和重放，这里有真实 CPU 可省。但**不能在函数最前面整体 return**，`ReconcileAuthoritativeSnapshot`（`BattleSimulation.cs:491`）开头三行必须保留执行：
  - `:498` `_authoritativeSnapshots.Save()` —— **S3 已在依赖它**
  - `:500` `DiscardPredictionsAtOrAfter()`
  - `:517-520` `if (logRollback) CapturePredictionErrorAfterReconciliation()` —— S1 的守卫，一致快照不重设平滑基线，否则每个快照都会重启平滑计时器。短路时别把这条守卫一起吞掉
  - 详细约定见 S3 计划「与 S5 / S6 的接口约定」一节，**动手前必读**
- **S7（击退，原 S6）需先复核 S1 阈值** —— 见下方反向约束

## 文档索引

**决策与现状**

| 文档 | 说明 |
|------|------|
| [物理层与玩法形态-讨论记录-20260803.md](物理层与玩法形态-讨论记录-20260803.md) | 三项决策的唯一出处（第五节）。与其他文档冲突时以本文为准 |
| [P1战斗沙盒-全景概览-2026-08-02.html](P1战斗沙盒-全景概览-2026-08-02.html) | 现在到底是什么样 + 一帧的生命周期 |
| [分支进度与SourceSDK借鉴分析-2026-08-02.html](分支进度与SourceSDK借鉴分析-2026-08-02.html) | 还差什么、为什么这么排序（§5、§7） |
| [Gameplay状态同步演示-Timeline.md](Gameplay状态同步演示-Timeline.md) | P0~P5 阶段划分（阶段≠执行顺序） |

**实现报告（08-04，反映 S1/S2 落地后的状态）**

| 文档 | 说明 |
|------|------|
| [报告/物理层详解-手写定点物理-20260804.html](报告/物理层详解-手写定点物理-20260804.html) | S2 后的物理层是怎么跑的 |
| [报告/网络同步分析报告-20260804.html](报告/网络同步分析报告-20260804.html) | 同步链路全景 |

**S1 / S2 / S3 施工文档（已完成，留作实现依据）**

| 文档 | 角色 |
|------|------|
| [S1-预测误差平滑与Ghost可视化-实现计划.md](S1-预测误差平滑与Ghost可视化-实现计划.md) | 做什么、6 个 Step。计划写四条新用例，实际落地五条（多 `error-smoothing-matching-snapshots-decay`） |
| [S1-实现交接说明.md](S1-实现交接说明.md) | 为什么这么做。九节都是设计理由 |
| [S2-物理层重写-手写定点物理-实现计划.md](S2-物理层重写-手写定点物理-实现计划.md) | 完整方案、5 个 Step、反射清除清单 |
| [../../Tools/AutomationAcceptance/预测误差平滑-验收测试指南-20260803.md](../../Tools/AutomationAcceptance/预测误差平滑-验收测试指南-20260803.md) | 验收用。基线哈希 `0xA565C15299DDC667` 是 S2 前的值，S2 后已合法变化 |
| [../../Tools/AutomationAcceptance/物理层重写-验收测试指南-20260803.md](../../Tools/AutomationAcceptance/物理层重写-验收测试指南-20260803.md) | 验收用。哈希基线会合法变化 |
| [S3-哈希上报与服务端告警-实现计划.md](S3-哈希上报与服务端告警-实现计划.md) | 7 个 Step。含精度修复、哈希口径决策表。**「与 S5 / S6 的接口约定」一节仍然生效，S6 动手前必读** |
| [../../Tools/AutomationAcceptance/哈希上报与服务端告警-验收测试指南-20260804.md](../../Tools/AutomationAcceptance/哈希上报与服务端告警-验收测试指南-20260804.md) | 验收用。**与 S1/S2 相反：哈希基线必须不变**。第 9 节两项真机观测待回填 |

**S4 施工文档（下一项）**

| 文档 | 角色 |
|------|------|
| [S4-弱网自动化测试-实现计划.md](S4-弱网自动化测试-实现计划.md) | 8 个 Step。含注入层选型、S3 哈希语义收窄建议（决策三，**实现前须定**）、确定性随机源约束。原 S7 |

> **dotnet 选哪个，看机器**：三个 csproj 是单目标 `net9.0`，需要一个带 net9.0 SDK 的 dotnet。家里那台装在 `C:/Users/shiwe/.dotnet/`（SDK 9.0.316），公司那台装在 `C:\Program Files\dotnet\`（9.0.305/312/315 + 10.0.201）。两份旧验收指南（20260803）原本写死了前者的全路径、断言后者「无 net9.0 SDK」——这只在家里成立，搬到公司就翻车。三份指南现已统一改为：先 `dotnet --list-sdks` 看 PATH 里的够不够，不够就换另一个全路径；选错会报 `NETSDK1045`。

**其他**

- [Box2D确定性分析/README.md](Box2D确定性分析/README.md) —— 部分被 08-03 决策取代
- [面试问题应对分析.md](面试问题应对分析.md) —— 07-27 基线过期，分档与应答思路仍有效
- [../状态帧同步/状态帧同步-阶段总目录.md](../状态帧同步/状态帧同步-阶段总目录.md) —— 底层内核版本线 v0.0a~v1.0，当前 v0.5a，与 S 编号是不同维度
- [_文档审计记录-20260803.md](_文档审计记录-20260803.md) —— 文档间冲突排查记录，日常不需要读
- 不涉及本分支：`../弹珠技术演示/`（已降级）、`../_归档/`、`../战斗Buff系统/`、`../技能节点编辑器/`
