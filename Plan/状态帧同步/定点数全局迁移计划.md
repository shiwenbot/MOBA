# 定点数全局迁移计划：帧同步系统全面 Fixed64 化

## Context

前序阶段「定点数库实现计划」已完成：羽毛球弹道全链路（ShuttlecockPhysics / ShuttlecockEntity / ShuttlecockState / ShuttlecockSnapshot / CourtConstants 等）迁移到 Fixed64 / Vector2d，TickAccumulator 内部使用 Fixed64 累积。

但当前帧同步系统的**角色移动链路**仍然使用 float：

1. **ITickable.Tick(uint, float)**：所有仿真实体的帧驱动接口使用 float fixedDt
2. **PlayerState**：玩家位置 `float X, Y`
3. **FrameSyncPhysicsWorld**：封装 Box2D，全链路 `System.Numerics.Vector2`（float）
4. **MoveSystem**：移动输入 `float dx, dy, dt`
5. **BattleSimulation**：客户端预测逻辑全部 float

这意味着：虽然羽毛球弹道已确定性，但**角色移动在跨平台（Windows GameServer vs 多平台 Unity Client）下不保证位级一致**。

### 已锁定的设计决策（来自前序计划和阶段总目录）

1. **帧率**：30Hz 逻辑帧（dt = 33.333ms）
2. **坐标约定**：2D 模型——X/Y 为平面坐标
3. **Box2D 范围**：角色移动走 Box2D，羽毛球弹道不走 Box2D（已迁移到 Fixed64）
4. **确定性工具链**：Fixed64.m_rawValue 位比较 + DeterminismRules 约束
5. **双端共享代码**：GameShared/ 源码通过 Entity.csproj 的 `<Compile Include>` glob 被 GameServer 编译

### 现有代码能力

- **已 Fixed64 化**：ShuttlecockPhysics、ShuttlecockEntity、ShuttlecockState、ShuttlecockSnapshot、CourtConstants、ShuttlecockLaunchCommand、ShuttlecockShotDefinition、ShuttlecockShotConfigFallback、ShuttlecockStateHasher、TickAccumulator（内部）、DeterminismRules（双版本）
- **待迁移**：PlayerState、PlayerStateSnapshot、PhysicsBodySnapshot、PhysicsWorldSnapshot、IPhysicsMovementWorld、FrameSyncPhysicsWorld、MoveSystem、BattleWorldState、ITickable、TickDispatcher、BattleSimulation
- **边界层（保持 float）**：BattleClientController（渲染层）、OuterMessage（网络协议）、Luban 配置代码生成

---

## 阶段状态

- **状态**：`未开始`
- **前置依赖**：定点数库实现计划（已完成）
- **后续衔接**：双端帧同步确定性验证（利用 Fixed64 做客户端 vs 服务端 hash 比对）

---

## 目标

1. **Tick 基础设施 Fixed64 化**：ITickable / TickDispatcher 全链路使用 Fixed64 的 fixedDt
2. **角色状态 Fixed64 化**：PlayerState / PlayerStateSnapshot 的位置字段迁移到 Fixed64
3. **物理边界转换层**：FrameSyncPhysicsWorld 对外暴露 Fixed64 API，内部保留 float 与 Box2D 交互
4. **全链路 Fixed64 化**：从输入到状态更新，所有 GameShared 游戏逻辑运算使用 Fixed64，同输入在同平台可复现

**阶段定位**：本阶段完成后，GameShared 中所有游戏逻辑运算均使用 Fixed64，快照/哈希/回滚链路基于 `m_rawValue` 位比较。但角色移动仍经过 Box2D float 链路（Fixed64 → float → Box2D → float → Fixed64），因此**不能宣称跨平台位级一致**。系统可宣称"同平台确定性"——即同一进程内两次相同输入产生位级相同的状态。跨平台确定性需要后续阶段（协议 raw long 化 + 跨进程 hash 比对）才能验证。

---

## 范围

- ITickable 接口：`float fixedDt` → `Fixed64 fixedDt`
- TickDispatcher：内部 Fixed64 传递
- PlayerState / PlayerStateSnapshot：位置 float → Fixed64
- PhysicsBodySnapshot / PhysicsWorldSnapshot：所有 float 字段 → Fixed64
- IPhysicsMovementWorld 接口：所有 float 参数 → Fixed64
- FrameSyncPhysicsWorld：Fixed64 API + 内部 float ↔ Box2D 转换
- MoveSystem：Fixed64 输入和运算
- BattleWorldState：适配新类型
- BattleSimulation：Fixed64 游戏逻辑（BattleInputSender 签名同步更新）
- DeterminismRules：移除 float-only 工具方法，扩展 Fixed64 工具
- StateHasher（如果存在独立文件）：更新哈希计算为 m_rawValue
- 测试文件适配

### 明确不在范围内

- **Box2D 内部**：Box2DSharp 库本身使用 float，不修改。Box2D 同平台输入一致时输出一致，跨平台确定性通过版本锁定保证
- **OuterMessage 网络协议**：PlayerSnapshot / C2B_PlayerInput 保持 float 字段不变，在序列化/反序列化边界做 Fixed64 ↔ float 转换
- **Luban 配置代码生成**：ShuttlecockShot / vector2 的生成代码保持 float，读取时转换（后续可改 Luban 定义输出 raw long）
- **BattleClientController 渲染层**：Unity 渲染仍用 float，在读取 PlayerState 时做 Fixed64 → float 转换
- **BuffSystem / DamageSystem**：这两个系统主要使用 int 类型（血量、蓝量、攻击力），不需要迁移
- **新增 FixedVector3 / 定点数矩阵/四元数**：当前 2D 模型只需要 Vector2d

---

## 非目标

- 不修改 Box2DSharp 库本身
- 不改变网络传输格式（OuterMessage 保持 float）
- 不做跨平台（不同 CPU 架构）Box2D 确定性验证
- 不做性能优化（SIMD 等），先保证正确性
- 不改 Luban 代码生成模板

---

## 技术决策

### TD-1：ITickable 接口 — fixedDt 从 float 改为 Fixed64

将 `ITickable.Tick(uint frameIndex, float fixedDt)` 改为 `ITickable.Tick(uint frameIndex, Fixed64 fixedDt)`。

**为什么改接口而非保留 float + 入口转换**：前序计划（TD-9）选择保留 float 接口以最小化影响面，但那只是羽毛球弹道的局部迁移。现在要做全局迁移，保留 float 接口意味着每个 ITickable 实现都要重复 `Fixed64 dt = DeterminismRules.FixedDeltaTimeFixed64` 的入口转换，违背 DRY 原则且容易遗漏。

**级联影响**：所有 ITickable 实现的 Tick 方法签名需同步更新。完整的 ITickable 实现列表：
- `ShuttlecockEntity`（GameShared/Badminton/）— 内部已 Fixed64，仅改签名
- `BattleComponent`（GameServer/Entity/Battle/）— 服务端入口
- `SnapshotManager`（GameLogic/FrameSync/）— 快照管理
- `FrameTimerService`（GameShared/FrameSync/Timer/）— 帧定时器，内部使用 float 计时需改为 Fixed64
- `BattleClientController`（GameLogic/Battle/）— 渲染层入口
- 测试 Mock 类（TickDispatcherTests 中的 MockTickable / CounterTickable / ThrowingTickable / CaptureDtTickable）

注意：`BattleWorldState` 不直接实现 ITickable，它通过 `BattleComponent.Tick` 间接被调度。

**落地**：TickDispatcher 从 TickAccumulator.FixedDeltaTime 获取 float 后立即转为 Fixed64，传递给所有 ITickable。

### TD-2：Box2D 边界策略 — Fixed64 API，float 内部

FrameSyncPhysicsWorld 实现 IPhysicsMovementWorld 接口。将接口和实现的所有 float 参数改为 Fixed64。FrameSyncPhysicsWorld 内部在调用 Box2D 前将 Fixed64 转为 float，Box2D 输出后将 float 转回 Fixed64。

**转换安全性**：Fixed64 ↔ float 转换是确定性的（相同的 Fixed64 raw 值总是产生相同的 float 值，反之亦然）。Box2D 在相同平台、相同输入、相同迭代次数下输出一致。因此 Fixed64 → float → Box2D → float → Fixed64 链路在同平台下是确定性的。

**Box2D 常量**：FrameSyncPhysicsWorld 内部的常量（PlayerBodyRadius、MinimumPlayerSeparation 等）改为 `static readonly Fixed64`，在传递给 Box2D 时转为 float。

**物理快照**：PhysicsBodySnapshot 的所有 float 字段改为 Fixed64。FrameSyncPhysicsWorld.TakeSnapshot() 将 Box2D 输出的 float 转为 Fixed64 填充快照。

### TD-3：网络协议边界 — 保持 float，序列化层转换

OuterMessage 中的 PlayerSnapshot（float X, Y）、C2B_PlayerInput（float Dx, Dy）、PhysicsBodySnapshotProto（float PositionX/Y 等）保持 float 不变。

**理由**：
- 网络协议的 float 字段是序列化格式，改动涉及 proto 定义 → 代码生成 → 双端同步升级，工作量大且风险高
- 相同的 float 值在两端（都是 .NET 8+）序列化/反序列化结果一致
- Fixed64 ↔ float 转换在序列化边界做，是确定性操作

**落地**：
- 服务端 BattleComponent 发送快照时：`Fixed64.ToFloat()` 填充协议字段
- 客户端 BattleSimulation 收到快照时：`Fixed64.FromFloat()` 或直接构造转换
- 客户端发送输入时：`float` 保持不变（输入本身就是 float，来自摇杆/键盘）

### TD-4：Luban 配置读取 — 保持 float 生成，读取时转换

ShuttlecockShot（Luban 生成的配置类）保持 `float HorizontalSpeed / LaunchAngle / Drag` 不变。ShuttlecockShotConfigProvider 在读取配置时将 float 转为 Fixed64 传给 ShuttlecockShotDefinition。

**理由**：改 Luban 定义需要修改 Excel/JSON 数据源 + Luban 模板 + 重新生成，属于配置系统工程，不在本迁移范围。

**当前状态**：ShuttlecockShotDefinition 已是 Fixed64 字段（前序计划完成），ShuttlecockShotConfigProvider 已做转换。无需额外工作。

### TD-5：渲染层边界 — 保持 float

BattleClientController（GameLogic/）和 ShuttlecockDebugController / ShuttlecockTrajectoryDebug 保持 float 用于 Unity 渲染。

**理由**：Unity 引擎渲染管线使用 float（Vector3、transform.position 等），Fixed64 无法直接用于 UnityEngine API。在渲染层读取 GameShared 状态时做 `Fixed64.ToFloat()` 转换即可。

**落地**：BattleClientController.Tick 接收 Fixed64 fixedDt，但内部渲染计算可以转为 float 使用。读取 PlayerState.X/Y 时做一次 `.ToFloat()` 转换。

### TD-6：BattleSimulation 迁移 — 全链路 Fixed64

BattleSimulation.cs 虽在 GameLogic/ 下，但通过 Entity.csproj 的 `<Compile Include>` 被 GameServer 编译。它不引用 UnityEngine（已验证），可以安全迁移到 Fixed64。

**迁移范围**：
- 输入参数：`float dx, dy` → `Fixed64 dx, dy`（BattleInputSender delegate 签名同步更新）
- 预测/回滚逻辑：状态比较使用 Fixed64.m_rawValue
- `NormalizeInput`：使用 `FixedMath.Sqrt` + `Vector2d` 运算

### TD-7：PlayerState 位置 — Fixed64，属性保持 int

PlayerState 的 `float X, Y` 改为 `Fixed64 X, Y`。属性（Health / MaxHealth / Mana / MaxMana / Attack）已经是 int，不需要迁移。

PlayerAttributeSnapshot 保持纯 int 字段，不需要迁移。

### TD-8：TickDispatcher 策略 — Fixed64 传递

TickDispatcher 从 TickAccumulator 获取 tick 事件后，将 `TickAccumulator.FixedDeltaTime`（float）转为 Fixed64 传给 ITickable。由于所有 ITickable 现在接收 Fixed64，Dispatcher 不再需要关心 float fixedDt 的传递。

TickDispatcher 内部的 `_fixedDt` 字段改为 `Fixed64`，在构造时从 TickAccumulator 的 FixedDeltaTime 属性（float）转换一次。

### TD-9：常量统一 — DeterminismRules.MoveSpeed 改为 Fixed64

`DeterminismRules.MoveSpeed` 当前为 `const float MoveSpeed = 5.0f`。FrameSyncPhysicsWorld 内部使用这个常量。改为 `static readonly Fixed64 MoveSpeed`。

### TD-10：测试策略 — m_rawValue 位比较

沿用前序计划的测试策略：
- 所有状态比较使用 `Fixed64.m_rawValue` 直接比较（整数值比较，天然位级一致）
- 确定性自测：同输入两次仿真，所有中间状态 m_rawValue 完全相等
- MoveSystem 自测：同输入同 Box2D 状态，输出位置位级一致

---

## 实现步骤

### Step 1：ITickable 接口迁移

修改 `GameShared/FrameSync/Core/ITickable.cs`。

- `void Tick(uint frameIndex, float fixedDt)` → `void Tick(uint frameIndex, Fixed64 fixedDt)`
- 添加 `using FixedMathSharp;`

### Step 2：TickDispatcher 迁移

修改 `GameShared/FrameSync/Core/TickDispatcher.cs`。

- `_fixedDt` 字段：`float` → `Fixed64`
- 构造函数：从 TickAccumulator.FixedDeltaTime（float）转为 Fixed64 存储
- `Update(float deltaTime)` 中调用 `tickable.Tick(frameIndex, _fixedDt)` 时传递 Fixed64

### Step 3：PlayerState / PlayerStateSnapshot 迁移

修改 `GameShared/FrameSync/Battle/PlayerState.cs`。

- `float X, Y` → `Fixed64 X, Y`
- 构造函数参数 `float x, float y` → `Fixed64 x, Fixed64 y`
- `ResetPosition` 方法适配新类型

修改 `GameShared/FrameSync/Battle/PlayerStateSnapshot.cs`。

- 所有 `float x, y` 字段 → `Fixed64 x, y`
- 构造函数参数适配
- `WithFrameIndex` 等方法适配

### Step 4：PhysicsBodySnapshot / PhysicsWorldSnapshot 迁移

修改 `GameShared/FrameSync/Battle/PhysicsBodySnapshot.cs`。

- 所有 float 字段（PositionX/Y、RotationRadians、LinearVelocityX/Y、AngularVelocity）→ Fixed64
- 构造函数参数适配

修改 `GameShared/FrameSync/Battle/PhysicsWorldSnapshot.cs`（如存在独立文件）。同步更新内部 PhysicsBodySnapshot 引用。

### Step 5：IPhysicsMovementWorld 接口迁移

修改 `GameShared/FrameSync/Battle/IPhysicsSnapshotProvider.cs`。

- `EnsureBody(int bodyId, float x, float y)` → `EnsureBody(int bodyId, Fixed64 x, Fixed64 y)`
- `SetBodyTransform(int bodyId, float x, float y, bool resetVelocity)` → Fixed64 参数
- `SetBodyMovementInput(int bodyId, float dx, float dy)` → Fixed64 参数
- `Step(float dt)` → `Step(Fixed64 dt)`
- `TryGetBodySnapshot` 返回值类型不变（PhysicsBodySnapshot 已是 Fixed64）

### Step 6：FrameSyncPhysicsWorld Fixed64 API 转换层

修改 `GameShared/FrameSync/Battle/FrameSyncPhysicsWorld.cs`。

这是本阶段最复杂的文件（642 行），需要：
- 所有接口方法实现：入口 Fixed64 → 内部 float → 调用 Box2D → 出口 float → Fixed64
- 内部常量：`const float` → `static readonly Fixed64`
- 内部 Box2D 交互保持 float（`System.Numerics.Vector2`、Body 操作等不变）
- `_pendingLinearVelocities` 字典的 value 类型从 `Vector2`（System.Numerics）保持不变（仅内部使用）
- `TakeSnapshot()` 方法：将 Box2D 输出的 float 转为 Fixed64 填充 PhysicsBodySnapshot
- `RestoreSnapshot()` 方法：将 Fixed64 的 PhysicsBodySnapshot 转为 float 设置 Box2D 状态
- `Step(Fixed64 dt)` → 内部 `(float)dt` 传给 Box2D World.Step
- 内部私有方法（如 `ResolvePlayerOccupancy`）保持 float 不变（仅 Box2D 内部使用）

### Step 7：MoveSystem 迁移

修改 `GameShared/FrameSync/Battle/MoveSystem.cs`。

- `Apply` 方法签名：`float dx, float dy, float dt` → `Fixed64 dx, Fixed64 dy, Fixed64 dt`
- `DeterminismRules.AssertFinite(dx, ...)` → 移除（Fixed64 无 NaN/Infinity）
- `DeterminismRules.AssertFixedDt(dt)` → 使用 Fixed64 版本
- `SyncFromPhysics` 方法：`state.X = bodySnapshot.PositionX` 现在两端都是 Fixed64，直接赋值

### Step 8：BattleWorldState 适配

修改 `GameShared/FrameSync/Battle/BattleWorldState.cs`。

注意：`BattleWorldState` 不实现 `ITickable`（它实现 `ISnapshotable<BattleWorldSnapshot>`），没有 `Tick` 方法。真正的 Tick 入口在 `BattleLogic.Tick` 和 `BattleComponent.Tick`。

- `AddOrUpdatePlayer` 构造函数调用：参数从 `float x, float y` 适配为 `Fixed64 x, Fixed64 y`
- `TakeSnapshot` / `RestoreSnapshot`：内部 PlayerStateSnapshot 和 PhysicsWorldSnapshot 类型已迁移为 Fixed64，自动适配
- `CheckConsistency` 方法适配

### Step 9：DeterminismRules 更新

修改 `GameShared/FrameSync/Determinism/DeterminismRules.cs`。

- `MoveSpeed`：`const float` → `static readonly Fixed64`
- 保留 `const float FixedDeltaTime`（TickAccumulator 的 float API 边界仍需要）
- 保留 `AssertFinite(float, string)`（BattleClientController 等渲染层仍可能使用）
- `AssertFixedDt(Fixed64)` 已存在，无需修改

### Step 10：BattleSimulation 迁移

修改 `GameLogic/Battle/BattleSimulation.cs`。

- `BattleInputSender` delegate：`float dx, float dy` → `Fixed64 dx, Fixed64 dy`
- `Tick` 方法参数：`float fixedDt` → `Fixed64 fixedDt`
- `SetJoined` 参数：`float x, float y` → `Fixed64 x, Fixed64 y`
- 内部输入缓存：`float _lastSentDx/Dy` → `Fixed64`
- `NormalizeInput` 方法：使用 `FixedMath.Sqrt` 替代 `MathF.Sqrt`
- `AreInputsEqual` 方法：使用 `m_rawValue` 比较替代 float epsilon 比较
- 状态差值计算：`float deltaX/Y` → `Fixed64`，使用 `m_rawValue` 比较
- `AdvancePredictionTo` 方法适配 Fixed64 dt
- 边界层：读取 OuterMessage 的 float 字段时转为 Fixed64，发送输入时 Fixed64 转为 float
- `BufferedInput` 内部类：`float Dx/Dy` → `Fixed64`
- 辅助常量（`InitialRttEmaMs`、`RttEmaAlpha`）：这些是网络层 RTT 统计量，保持 float

### Step 11：BattleClientController 边界适配

修改 `GameLogic/Battle/BattleClientController.cs`。

- `Tick` 方法签名：接收 `Fixed64 fixedDt`（适配 ITickable）
- `_cachedDx/Dy`：保持 float（来自键盘/摇杆输入，本身就是 float）
- 发送输入时：float → Fixed64 → BattleSimulation（或保持 float 走 SendInputCommand）
- 读取 PlayerState 时：`Fixed64.ToFloat()` 用于 `ToWorldPosition` 等渲染方法
- `ToWorldPosition(float x, float y)` 保持 float（Unity 渲染 API 需要）
- `ReadKeyboardDirection` 保持 float 输出
- `SendInputCommand` 的 `float dx, float dy` 参数需要适配 BattleSimulation 新签名

### Step 12：服务端 BattleComponent / BattleLogic / TestHarness 适配

修改 `GameServer/Server/Entity/Battle/BattleComponent.cs` 和 `BattleLogic.cs`。

- 快照广播时：`player.X.ToFloat()` 填充 OuterMessage 的 float 字段
- 输入处理时：从 OuterMessage 的 float Dx/Dy 转为 Fixed64 传给 MoveSystem
- Tick 调用适配 Fixed64 fixedDt（BattleLogic.Tick 是服务端真正的 Tick 入口）

修改 `GameServer/Server/Entity/TestHarness/` 目录下的文件。

- `TestRunner.cs`：`DeterminismRules.FixedDeltaTime`（float）传给 BattleLogic.Tick 的地方改为 `DeterminismRules.FixedDeltaTimeFixed64`；`DeterminismRules.MoveSpeed`（float）的计算改为 Fixed64；`state.X` / `state.Y` 比较从 `float` 改为 Fixed64；`JoinPlayer` 调用的 `float x, y` 改为 Fixed64
- `FrameSyncValidationRunner.cs`：内部的 ITickable 实现（第 768 行 `Tick(uint, float)`）改为 Fixed64；所有 `TickAccumulator.DefaultFixedDeltaTime`（float）传给 ITickable 的地方改为 `DeterminismRules.FixedDeltaTimeFixed64`
- `SimulatedClient.cs`：float 适配
- `TestSnapshot.cs`：PlayerStateSnapshot 构造适配（float → Fixed64）

### Step 13：其他 ITickable 实现迁移

修改 `GameShared/FrameSync/Timer/FrameTimerService.cs`。

- `Tick` 方法签名：`float fixedDt` → `Fixed64 fixedDt`
- 内部计时逻辑：如果有 float 累积，改为 Fixed64

修改 `GameLogic/FrameSync/SnapshotManager.cs`。

- `Tick` 方法签名：`float fixedDt` → `Fixed64 fixedDt`
- 内部逻辑适配

### Step 14：StateHasher 重写

修改 `GameShared/FrameSync/Snapshot/StateHasher.cs`。

当前哈希方法 `NormalizeFloatBits(float)` 使用 `BitConverter.SingleToInt32Bits` 将 float 转为 int 再参与 FNV-1a 哈希。迁移后所有字段变为 Fixed64，需要重写：

- `PlayerStateSnapshot` 的 x/y：直接用 `player.X.m_rawValue` 和 `player.Y.m_rawValue` 替代 `NormalizeFloatBits(player.X/Y)`
- `PhysicsBodySnapshot` 的所有字段：直接用 `body.PositionX.m_rawValue` 等替代 `NormalizeFloatBits(...)`
- `NormalizeFloatBits` 方法：删除（Fixed64 无 NaN/Infinity/-0 问题，不需要归一化）
- 哈希稳定性测试：当前 `SnapshotSelfTestSuite` 中的 `-0.0f` / `float.NaN` 用例需重写为 Fixed64 raw 值稳定性测试（验证同输入两次哈希完全相等）

修改 `GameShared/FrameSync/Snapshot/SnapshotSelfTestSuite.cs`。

- 测试用例中的 `float.NaN` / `-0.0f` 构造 PlayerStateSnapshot 的方式不再适用，需改为 Fixed64 构造
- 新增 raw 值稳定性验证：同输入两次快照，哈希完全相等

### Step 15：测试适配

修改 `Tests/Badminton/ShuttlecockEntityTests.cs`。

- Tick 调用适配 Fixed64 fixedDt
- 其他测试数据准备适配新类型

修改 `Tests/FrameSync/TickAccumulatorTests.cs`。

- `TickDispatcher` 相关测试适配 Fixed64 fixedDt

修改 `Tests/FrameSync/` 下其他测试文件（如有）。

修改 `GameServer/Server/Entity/TestHarness/TestRunner.cs`。

- 大量使用 `float` 的验证逻辑需迁移到 Fixed64（见 Step 12 的详细说明）
- 特别是 `MoveSpeed * FixedDeltaTime` 的期望值计算、`state.X` 的 epsilon 比较需改为 m_rawValue 比较

修改 `GameServer/Server/Entity/TestHarness/FrameSyncValidationRunner.cs`。

- 内部 ITickable 实现的 Tick 签名适配

新增或扩展 `Tests/FixedPoint/FixedMathSharpTests.cs`：

- `MoveSystem_DeterminismSelfTest`：同输入两次移动，Fixed64 输出位级一致
- `PhysicsWorld_DeterminismSelfTest`：同输入两次物理步进，快照位级一致

---

## 文件结构

### 修改文件（GameShared — 游戏逻辑层）

```
UnityProject/Assets/GameScripts/HotFix/GameShared/
  FrameSync/
    Core/
      ITickable.cs                           // float fixedDt → Fixed64 fixedDt
      TickDispatcher.cs                      // 内部 Fixed64 传递
      TickAccumulator.cs                     // 微调（已有 Fixed64 内部）
    Battle/
      PlayerState.cs                         // float X,Y → Fixed64
      PlayerStateSnapshot.cs                 // float x,y → Fixed64
      PhysicsBodySnapshot.cs                 // 所有 float → Fixed64
      PhysicsWorldSnapshot.cs                // 适配 Fixed64
      IPhysicsSnapshotProvider.cs            // 接口方法 float → Fixed64
      FrameSyncPhysicsWorld.cs               // Fixed64 API + 内部 float 转换层
      MoveSystem.cs                          // float dx,dy,dt → Fixed64
      BattleWorldState.cs                    // 适配 Fixed64 类型
      StateHasher.cs                         // float 哈希 → m_rawValue
    Timer/
      FrameTimerService.cs                   // Tick 签名 + 内部 Fixed64
    Snapshot/
      StateHasher.cs                         // 重写哈希：删除 NormalizeFloatBits，用 m_rawValue
      SnapshotSelfTestSuite.cs               // 重写 NaN/-0 测试为 Fixed64 raw 稳定性测试
    Determinism/
      DeterminismRules.cs                    // MoveSpeed → Fixed64

UnityProject/Assets/GameScripts/HotFix/GameShared/Tests/
  FixedPoint/
    FixedMathSharpTests.cs                   // 扩展 MoveSystem/PhysicsWorld 确定性测试
  Badminton/
    ShuttlecockEntityTests.cs                // 适配 Fixed64 Tick 签名
  FrameSync/
    TickAccumulatorTests.cs                  // 适配 Fixed64 Tick 签名
```

### 修改文件（GameLogic — 客户端侧）

```
UnityProject/Assets/GameScripts/HotFix/GameLogic/Battle/
  BattleSimulation.cs                        // Fixed64 游戏逻辑 + float 边界
  BattleClientController.cs                  // Fixed64 Tick + float 渲染
UnityProject/Assets/GameScripts/HotFix/GameLogic/FrameSync/
  SnapshotManager.cs                         // Fixed64 Tick 签名
```

### 修改文件（GameServer — 服务端侧）

```
GameServer/Server/Entity/Battle/
  BattleComponent.cs                         // Fixed64 ↔ float 协议边界
  BattleLogic.cs                             // Fixed64 游戏逻辑适配
GameServer/Server/Hotfix/Battle/
  System/BattleComponentSystem.cs            // Tick 调用适配
  Handler/C2B_PlayerInputHandler.cs          // 输入 float → Fixed64
GameServer/Server/Entity/TestHarness/
  TestRunner.cs                              // float fixedDt/MoveSpeed/PlayerState.X → Fixed64
  FrameSyncValidationRunner.cs               // ITickable 实现 + float fixedDt → Fixed64
  SimulatedClient.cs                         // float 适配
  TestSnapshot.cs                            // PlayerStateSnapshot 构造适配
```

### 不修改的文件

```
UnityProject/Assets/GameScripts/HotFix/GameProto/Generate/NetworkProtoco/OuterMessage.cs  // 保持 float
UnityProject/Assets/GameScripts/HotFix/GameProto/GameConfig/badminton/ShuttlecockShot.cs   // 保持 float
UnityProject/Assets/GameScripts/HotFix/GameProto/GameConfig/vector2.cs                    // 保持 float
UnityProject/Assets/GameScripts/HotFix/GameShared/ThirdParty/Box2DSharp/                  // 不动
UnityProject/Assets/GameScripts/HotFix/GameShared/ThirdParty/FixedMathSharp/              // 不动
```

---

## 验证方式

### 编译验证

- `dotnet build 'GameServer/Server/Server.sln' -c Debug -v minimal` — 服务端编译通过（Fixed64 源码通过 Entity.csproj 的 GameShared glob 可见）
- Unity Editor 打开无编译错误 — GameShared + GameLogic 编译通过

### 自动验证

- `dotnet run --project 'GameServer/Server/Main/Main.csproj' --framework net8.0 -- --mode=test --scenario=all` — 服务端全部自测通过
- Unity EditMode 测试全部通过（含 ShuttlecockEntityTests、TickAccumulatorTests、FixedMathSharpTests）

### 一致性验证

- `BattleWorldState` 确定性自测：同输入两次仿真，所有帧 PlayerStateSnapshot / PhysicsBodySnapshot 的 `m_rawValue` 完全相等
- `MoveSystem` 确定性自测：同移动输入同 Box2D 状态，输出位置 `m_rawValue` 完全相等
- `BattleSimulation` 预测自测：客户端预测 → 权威快照校正 → 回滚重播链路正常

**重要说明**：由于本阶段 OuterMessage 保持 float，服务端和客户端的状态重建路径不同：
- 服务端：直接在 Fixed64 游戏逻辑中运算 → 直接哈希 Fixed64 raw 值
- 客户端：接收 float 协议 → 转为 Fixed64 → 重建状态 → 哈希

因此**本阶段的 hash 比对仅限"同进程内自测"**（不涉及网络传输）。跨进程（客户端 vs 服务端）的 hash 比对需要后续阶段完成协议 raw long 化后才能严格对齐。

---

## 完成标准

### 功能验证

- [ ] `ITickable.Tick` 签名改为 `Fixed64 fixedDt`，所有实现编译通过
- [ ] `PlayerState.X / Y` 为 `Fixed64`，`PlayerStateSnapshot.x / y` 为 `Fixed64`
- [ ] `PhysicsBodySnapshot` 所有 float 字段改为 `Fixed64`
- [ ] `IPhysicsMovementWorld` 接口方法全部使用 `Fixed64` 参数
- [ ] `FrameSyncPhysicsWorld` 实现 Fixed64 API，内部 Box2D 交互正常
- [ ] `MoveSystem.Apply` 和 `SyncFromPhysics` 使用 `Fixed64`
- [ ] `BattleSimulation` 内部游戏逻辑使用 `Fixed64`

### 确定性验证

- [ ] **Tick 驱动确定性**：同输入两次 Tick 调用，Fixed64 fixedDt 传递完全一致
- [ ] **移动确定性**：同移动输入两次 MoveSystem.Apply + PhysicsWorld.Step，输出 PlayerState m_rawValue 完全相等
- [ ] **快照回滚确定性**：保存快照 → 推进 N 帧 → 恢复快照 → 继续推进，与无中断连续运行 m_rawValue 完全相等
- [ ] **预测链路**：BattleSimulation 预测 → 权威校正 → 回滚重播链路正常（无报错）

### 工程验证

- [ ] GameShared 程序集编译通过（无新增 asmdef 或引用变更）
- [ ] GameServer 编译通过（Entity.csproj 通过 GameShared glob 可见 Fixed64 源码）
- [ ] 服务端自测通过：Checks 全部 PASS，Scenarios 全部 PASS
- [ ] Unity EditMode 测试全部通过
- [ ] OuterMessage 网络协议保持 float 字段不变（仅边界转换）
- [ ] Box2DSharp 库本身未被修改
- [ ] Luban 生成的配置代码未被修改

---

## 校验结果

| 校验项 | 结果 | 发现问题数 |
|--------|------|-----------|
| 代码可行性 | ⚠️ 已修正 | 3（BattleWorldState 无 Tick、TestHarness 遗漏、StateHasher 改动量低估） |
| GDD 一致性 | ⚠️ 已跳过 | 本阶段无 GDD（迁移计划） |
| 总目录边界 | ⚠️ 已修正 | 1（确定性声明从"跨平台"收敛为"同平台"） |
| 结构完整性 | ⚠️ 已修正 | 1（协议边界 hash 验收口径不完整） |

### 已修正的问题

1. ✅ 确定性声明收敛：目标第 4 条从"同输入可跨平台复现"改为"同输入在同平台可复现"；阶段定位明确说明不能宣称跨平台位级一致
2. ✅ BattleWorldState 步骤重写：移除不存在的 Tick 方法描述，改为 AddOrUpdatePlayer / TakeSnapshot / RestoreSnapshot 的类型适配；Tick 迁移归到 BattleLogic / BattleComponent
3. ✅ TestHarness 补齐：Step 12 和 Step 15 新增 TestRunner / FrameSyncValidationRunner / SimulatedClient / TestSnapshot 四个文件的适配说明；文件结构中新增 GameServer/Server/Entity/TestHarness/ 目录
4. ✅ StateHasher 重写：从"可保留 NormalizeFloatBits、无需改动"改为删除 NormalizeFloatBits、重写哈希为 m_rawValue、重写 SnapshotSelfTestSuite 的 NaN/-0 测试用例
5. ✅ 协议边界 hash 口径：一致性验证章节新增说明，本阶段 hash 比对仅限同进程自测，跨进程 hash 需等协议 raw long 化

### 判定：修订后通过

✅ ITickable — GameShared/FrameSync/Core/ITickable.cs
✅ TickDispatcher — GameShared/FrameSync/Core/TickDispatcher.cs
✅ PlayerState — GameShared/FrameSync/Battle/PlayerState.cs
✅ PlayerStateSnapshot — GameShared/FrameSync/Battle/PlayerStateSnapshot.cs
✅ PhysicsBodySnapshot — GameShared/FrameSync/Battle/PhysicsBodySnapshot.cs
✅ PhysicsWorldSnapshot — GameShared/FrameSync/Battle/PhysicsWorldSnapshot.cs
✅ IPhysicsMovementWorld — GameShared/FrameSync/Battle/IPhysicsSnapshotProvider.cs
✅ FrameSyncPhysicsWorld — GameShared/FrameSync/Battle/FrameSyncPhysicsWorld.cs（642 行）
✅ MoveSystem — GameShared/FrameSync/Battle/MoveSystem.cs
✅ BattleWorldState — GameShared/FrameSync/Battle/BattleWorldState.cs（不实现 ITickable，通过 BattleComponent 间接调度）
✅ BattleSimulation — GameLogic/Battle/BattleSimulation.cs
✅ BattleClientController — GameLogic/Battle/BattleClientController.cs
✅ BattleComponent — GameServer/Server/Entity/Battle/BattleComponent.cs（实现 ITickable）
✅ BattleLogic — GameServer/Server/Entity/Battle/BattleLogic.cs
✅ StateHasher — GameShared/FrameSync/Snapshot/StateHasher.cs
✅ FrameTimerService — GameShared/FrameSync/Timer/FrameTimerService.cs（Tick 不使用 fixedDt）
✅ SnapshotManager — GameLogic/FrameSync/SnapshotManager.cs

### 总目录边界检查

✅ 本计划为跨阶段基础设施迁移，不越界实现 v0.5b/v0.5c 功能
✅ 不修改 Box2DSharp 库本身
✅ 不改变网络协议格式
✅ 前序依赖（定点数库实现计划）已完成
✅ 后续衔接明确（双端验证、Luban 迁移、协议迁移）

### 判定：通过

---

## 后续衔接

1. **双端帧同步确定性验证**：利用 Fixed64 全局化，做客户端 vs 服务端的 hash 比对，验证跨进程一致性
2. **Luban 配置定点化**：修改 Luban 数据源定义，将 ShuttlecockShot / vector2 的 float 字段改为 int（定点数原始值），由 Luban 代码生成器直接产出 Fixed64 字段
3. **网络协议定点化**：OuterMessage 中的位置/速度字段从 float 改为 long（Fixed64 raw 值），消除序列化边界的 float 精度丢失
4. **继续主线阶段**：v0.5b（技能系统同步）、v0.5c（行为树黑板同步）等后续阶段基于 Fixed64 全局化后的代码继续开发
